import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles((theme) => ({
  root: {
    marginTop: "40px",
    flex: "1 1 auto",
    marginTop: 100,
    marginBottom: 70,
  },
}));

export default useStyles;
