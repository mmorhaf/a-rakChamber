import { makeStyles } from "@material-ui/styles";

const useAStyles = makeStyles((theme) => ({
  Accordion: {
    fontSize: "18px",
    fontWeight: "700",
    color: "#263661",
  },
}));
export default useAStyles;
